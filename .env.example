# BloodFlow Manager public URL (Railway / Render / custom domain)
# After deploy, set this in Vercel → Project → Settings → Environment Variables
# Example: https://bloodflow-manager.up.railway.app
VITE_BLOODFLOW_URL=
